<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
             x:Class="MsorLi.Views.ProfilePage"
             xmlns:controls="clr-namespace:ImageCircle.Forms.Plugin.Abstractions;assembly=ImageCircle.Forms.Plugin.Abstractions"
             xmlns:control="clr-namespace:Xamarin.Forms;assembly=Xamarin.Forms.CarouselView"
             >
	<ContentPage.Content>
        <Grid>

        <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="6*"/>
            </Grid.RowDefinitions>



            <StackLayout Grid.Row="0" BackgroundColor="#00BCD4">
                <StackLayout VerticalOptions="CenterAndExpand">
                    <controls:CircleImage x:Name="UserImg" BorderColor="White" BorderThickness="2" HorizontalOptions="Center" VerticalOptions="End" WidthRequest="140" HeightRequest="140"/>
                    <Label x:Name="UserName" Font="Bold" HorizontalOptions="Center" Margin="0,0,0,0" VerticalOptions="End" />
                </StackLayout>
            </StackLayout>


                <Grid BackgroundColor="Silver" Grid.Row="1">
                    
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <Label Grid.Column="0" Text="מוצרים שפרסמתי" HorizontalOptions="Center" VerticalOptions="Center"/>
                    <Label Grid.Column="1" Text="מוצרים שאהבתי" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Grid>

            
            <ScrollView Grid.Row="2" Orientation="Horizontal">
                <ListView x:Name="listView_items" Rotation="270" SeparatorVisibility="None">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Image Rotation="-270" Source="{Binding Item1}">
                                    
                                </Image>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    
                </ListView>
              <!--    <StackLayout Orientation="Horizontal">
                  
                    <Image Grid.Column="0" Aspect="AspectFill" Margin="0,4,2,0" Source="{Binding Item1}">
                            <Image.GestureRecognizers>
                                <TapGestureRecognizer Tapped="OnSelection" CommandParameter="{Binding Item2}">
                                </TapGestureRecognizer>
                            </Image.GestureRecognizers>
                    </Image>
                    
                </StackLayout>-->
            </ScrollView>

            


            
            



            
        </Grid>

	</ContentPage.Content>
</ContentPage>
